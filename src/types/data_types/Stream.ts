export module Stream {
    export interface StreamInfo {
        source: string;
        name?: any;
        author?: string;
        authorImgUrl?: string;
        genre?: string
        platform?: string;
        length?: string;
        thumbnailUrl?: string;
    }
}